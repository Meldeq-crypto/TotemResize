{
  "schemaVersion": 1,
  "id": "totemresize",
  "version": "${version}",
  "name": "Totem Resizer",
  "description": "Client-side Totem of Undying resizer with an Interactive Canvas visual editor. Features corner-drag resizing, dual-view tabs for held position and pop animation, resource pack synergy (multiplies rather than overrides), and Matrix4f-based transforms. Uses the provided totem_of_undying.json as the baseline at slider value 5 (1.0×).",
  "authors": [
    "YourName"
  ],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": [
      "totemresize.client.TotemResizeClient"
    ],
    "modmenu": [
      "totemresize.config.ModMenuIntegration"
    ]
  },
  "mixins": [
    "totemresize.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.16.0",
    "fabric": "*",
    "minecraft": ">=1.21.1 <1.22"
  },
  "suggests": {
    "modmenu": "*"
  }
}
